#include<bits/stdc++.h>
using namespace std;
 
int main()
{
    int t;
    cin >> t;
    while(t--)
    {
       set<int>st;
 
       for(int i=0 ; i<4;i++)
       {
          int temp;
          cin >> temp;
          st.insert(temp);
       }
        
       if(st.size()==1)
       {
        cout<<"YES"<<endl;
        
       }
 
       else cout<<"NO"<<endl;
    }
}